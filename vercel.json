{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/.*", "dest": "/api/$0" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "crons": [
    {
      "path": "/api/send-access-codes",
      "schedule": "0 10 * * *"
    }
  ]
}
